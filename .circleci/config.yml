version: 2
jobs:
  test:
    working_directory: ~/code
    docker:
      - image: circleci/python:3.6
    steps:
      - checkout
      - run:
          name: run tests
          command: |
            pip install tox-pipenv
            tox -r

workflows:
  version: 2
  build_and_package:
    jobs:
      - test
